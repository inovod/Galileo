import { SurtidoSortableDirective } from './surtido-sortable.directive';

describe('SurtidoSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new SurtidoSortableDirective();
    expect(directive).toBeTruthy();
  });
});
